#user container-based infrastructure
sudo: false
before_install:
# install dependencies
install: "pip install -r requirements.txt"  
language: python
python:
  - "2.7"
script: robot --consolecolors on -L TRACE:INFO -A suites/dozorro/arguments.txt suites/dozorro/suite.robot
